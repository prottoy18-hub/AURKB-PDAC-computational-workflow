# Recorded package versions, by execution stage.
#
# This file is a RECORD, not an installation file. The three notebooks ran in three different
# environments and their pinned versions are mutually incompatible in a single environment.
# Install the block matching the stage you intend to run. Lines marked "not directly recorded"
# had no preserved version and must not be guessed.
#
# Evidence: notebook output cells; Provenance/package_versions.json and
# Provenance/environment_information.json of run_20260715T124827Z_79cebaa2; joblib serialization
# metadata of the archived model artifacts. See environment/environment_versions.md.

# ---------------------------------------------------------------------------
# Stage 1 - Notebook 1, COCONUT prescreening (local Windows / VS Code)
# python==3.13.5
# ---------------------------------------------------------------------------
pandas==2.2.3
numpy==2.2.5
rdkit==2026.03.2
# tqdm: not directly recorded (imported by scripts/aurkb_prescreening_vscode.py)
# matplotlib: not directly recorded (used for the chemical-space figures)
# pyarrow: not installed at run time; Parquet output was skipped and logged

# ---------------------------------------------------------------------------
# Stage 2 - Notebook 2, classical QSAR reproduction (local Windows / conda env research-py312)
# python==3.12.13
# ---------------------------------------------------------------------------
# numpy==2.3.5
# pandas==2.2.3
# scikit-learn==1.8.0
# xgboost==3.1.3
# rdkit==2025.9.4
# joblib==1.5.3
# matplotlib==3.10.8
# nbformat==5.10.4

# ---------------------------------------------------------------------------
# Stage 2b - the ORIGINAL historical QSAR training environment (partially recoverable)
# ---------------------------------------------------------------------------
# scikit-learn==1.6.1        # recovered from joblib serialization metadata of the archived models
# xgboost: not directly recorded
# python: not directly recorded
# rdkit: not directly recorded for this stage

# ---------------------------------------------------------------------------
# Stage 3 - Notebook 3, DeepChem GCN and consensus screening (Google Colab / Linux)
# python==3.12.13
# TF_USE_LEGACY_KERAS=1
# ---------------------------------------------------------------------------
# numpy==1.26.4
# pandas==2.2.2
# scikit-learn==1.6.1
# tensorflow==2.20.0
# deepchem==2.8.1.dev
# rdkit==2026.03.2
